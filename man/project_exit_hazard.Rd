% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazard_models_utils.R
\name{project_exit_hazard}
\alias{project_exit_hazard}
\title{Project Non-Retirement Exit Events for a Simulation Snapshot}
\usage{
project_exit_hazard(
  panel_contract_dt,
  panel_personnel_dt,
  sim_contract_dt,
  sim_personnel_dt,
  use_hazard_model = TRUE,
  active_types = NULL,
  exit_rate = NULL,
  freq = NULL,
  extra_covariates = NULL,
  threshold_method = "f1",
  age_col = "age",
  tenure_col = "tenure_years",
  personnel_id_col = "personnel_id",
  ref_date_col = "ref_date",
  birth_date_col = "birth_date",
  start_date_col = "start_date",
  end_date_col = "end_date",
  contract_type_col = "contract_type_code",
  contract_id_col = "contract_id",
  salary_col = "gross_salary_lcu",
  ref_date = NULL
)
}
\arguments{
\item{panel_contract_dt}{data.table.  Historical contract panel (all
snapshots stacked) used to build the training dataset.}

\item{panel_personnel_dt}{data.table.  Historical personnel panel (all
snapshots stacked).}

\item{sim_contract_dt}{data.table.  Single-period simulation contract
snapshot to score.}

\item{sim_personnel_dt}{data.table.  Single-period simulation personnel
snapshot.}

\item{use_hazard_model}{Logical.  Default \code{TRUE}.  When \code{TRUE},
a binomial GLM is estimated and used to predict exits.  When
\code{FALSE}, a flat \code{exit_rate} random draw is applied (requires
\code{exit_rate} to be non-\code{NULL}).}

\item{active_types}{Character vector or \code{NULL}.  Contract type codes
considered active (at-risk).  \code{NULL} (default) uses all types except
\code{"inactive"} and \code{"pensioner"}.}

\item{exit_rate}{Numeric scalar or \code{NULL}.  Annual exit probability
applied on the FALSE path (e.g. \code{0.05}).  Ignored when
\code{use_hazard_model = TRUE}.}

\item{freq}{Character scalar or \code{NULL}.  Panel cadence for
\code{govhr::detect_personnel_event()}.  \code{NULL} auto-infers.}

\item{extra_covariates}{Character vector or \code{NULL}.  Additional columns
to include as model covariates.  Default: \code{NULL}.}

\item{threshold_method}{Character.  One of \code{"youden"} or \code{"f1"}
(default).  \code{"f1"} is preferred for exit modelling because public
sector exits are typically rare events (\eqn{<5\%} base rate), where
precision matters more than joint sensitivity/specificity balance.}

\item{age_col}{Character or \code{NULL}.  Default: \code{"age"}.}

\item{tenure_col}{Character or \code{NULL}.  Default: \code{"tenure_years"}.}

\item{personnel_id_col}{Character.  Default: \code{"personnel_id"}.}

\item{ref_date_col}{Character.  Default: \code{"ref_date"}.}

\item{birth_date_col}{Character.  Default: \code{"birth_date"}.}

\item{start_date_col}{Character.  Default: \code{"start_date"}.}

\item{end_date_col}{Character.  Default: \code{"end_date"}.}

\item{contract_type_col}{Character.  Default: \code{"contract_type_code"}.}

\item{contract_id_col}{Character.  Default: \code{"contract_id"}.}

\item{salary_col}{Character.  Default: \code{"gross_salary_lcu"}.}

\item{ref_date}{Date scalar or \code{NULL}.  Snapshot date of
\code{sim_contract_dt}, used on the slow age/tenure computation path.
Default: \code{NULL}.}
}
\value{
A \code{data.table} with one row per active person in
\code{sim_contract_dt}:
\describe{
\item{\code{personnel_id_col}}{Person identifier.}
\item{\code{prob}}{Predicted exit probability (TRUE path) or
\code{NA_real_} (FALSE path).}
\item{\code{event}}{Integer 0/1.  1 = predicted exiter this period.}
}
On the TRUE path, the fitted model is stored as
\code{attr(result, "hazard_model")}.
}
\description{
End-to-end non-retirement exit projection for one simulation period.
When \code{use_hazard_model = TRUE} (the default), a discrete-time hazard
model is estimated from the historical panel and scored on the current
simulation snapshot.  When \code{use_hazard_model = FALSE}, a flat
\code{exit_rate} is applied and exiters are selected at random.
}
\seealso{
\code{\link{build_exit_hazard_data}}, \code{\link{fit_hazard_model}},
\code{\link{select_hazard_threshold}}, \code{\link{predict_hazard}}
}
