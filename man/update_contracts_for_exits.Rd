% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exit_update.R
\name{update_contracts_for_exits}
\alias{update_contracts_for_exits}
\title{Update Contracts for Non-Retirement Exits}
\usage{
update_contracts_for_exits(
  contract_dt,
  exits_dt,
  ref_date,
  personnel_id_col = "personnel_id",
  contract_type_col = "contract_type_code",
  end_date_col = "end_date",
  active_types = "active",
  exited_type = "inactive"
)
}
\arguments{
\item{contract_dt}{data.table.  Contract data (modified by reference).}

\item{exits_dt}{data.table.  Table of exiting personnel — must contain
\code{personnel_id_col}.}

\item{ref_date}{Date.  Reference date; becomes the exit date.}

\item{personnel_id_col}{Character.  Default \code{"personnel_id"}.}

\item{contract_type_col}{Character.  Default \code{"contract_type_code"}.}

\item{end_date_col}{Character.  Default \code{"end_date"}.}

\item{active_types}{Character vector.  Contract type values to update.}

\item{exited_type}{Character.  Value to write into \code{contract_type_col}.
Default \code{"inactive"}.}
}
\value{
\code{contract_dt} (invisibly; modified by reference in place).
}
\description{
Marks ALL active contracts for each exiting person as \code{exited_type}
and sets \code{end_date_col} to \code{ref_date}.  Uses a plain membership
filter on \code{personnel_id} so that multi-contract persons are handled
correctly in one pass.
}
\section{Data Integrity}{

Modifies \code{contract_dt} \strong{in place} via data.table reference semantics.
The caller's table is changed; pass \code{data.table::copy()} if the
original must be preserved.
}

\keyword{internal}
