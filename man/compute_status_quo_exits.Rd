% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exit_core.R
\name{compute_status_quo_exits}
\alias{compute_status_quo_exits}
\title{Select Personnel for Non-Retirement Exit Under Status-Quo Policy}
\usage{
compute_status_quo_exits(
  contract_dt,
  exit_rates_dt,
  group_cols = NULL,
  exit_strategy = "random",
  exit_multiplier = 1,
  personnel_id_col = "personnel_id",
  contract_type_col = "contract_type_code",
  active_types = "active"
)
}
\arguments{
\item{contract_dt}{data.table.  Current (single-snapshot) contract data.}

\item{exit_rates_dt}{data.table.  Output of
\code{estimate_historical_exit_rates()} — must contain \code{group_cols}
and \code{exit_rate}.}

\item{group_cols}{Character vector or \code{NULL}.  Grouping columns.
If \code{NULL}, a single overall exit rate is applied.}

\item{exit_strategy}{Character.  \code{"random"} (default) or the name of
a numeric column in \code{contract_dt} to rank by (ascending — lowest
values exit first).}

\item{exit_multiplier}{Numeric.  Scale the historical rate up or down.
Default \code{1.0}.}

\item{personnel_id_col}{Character.  Default \code{"personnel_id"}.}

\item{contract_type_col}{Character.  Default \code{"contract_type_code"}.}

\item{active_types}{Character vector.  Contract type values treated as
active (non-pensioner) eligible for exit.  Default \code{"active"}.}
}
\value{
data.table with at least \code{personnel_id_col} column identifying
personnel selected to exit this period.
}
\description{
Applies historical exit rates to the current workforce by group.
Returns a data.table of personnel IDs selected to exit this period.
Selection within each group follows \code{exit_strategy} (random or any
numeric column in \code{contract_dt}).
}
\keyword{internal}
