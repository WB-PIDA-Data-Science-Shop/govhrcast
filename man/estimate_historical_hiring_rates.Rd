% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hiring_core.R
\name{estimate_historical_hiring_rates}
\alias{estimate_historical_hiring_rates}
\title{Estimate Historical Hiring Rates from Panel Data}
\usage{
estimate_historical_hiring_rates(
  panel_contract_dt,
  panel_personnel_dt,
  group_cols,
  freq = "year",
  personnel_id_col = "personnel_id",
  ref_date_col = "ref_date",
  start_date_col = "start_date",
  end_date_col = "end_date",
  contract_type_col = "contract_type_code",
  status_col = "status"
)
}
\arguments{
\item{panel_contract_dt}{data.table. Full panel of contract data (all ref_dates).}

\item{panel_personnel_dt}{data.table. Full panel of personnel data (all ref_dates).}

\item{group_cols}{Character vector. Columns to group by (e.g. \code{"est_id"}).
Pass \code{NULL} for overall (ungrouped) rates.}

\item{freq}{Character. Frequency passed to \code{govhr::detect_personnel_event()}.
Default \code{"year"}.}

\item{personnel_id_col}{Character. Personnel ID column. Default \code{"personnel_id"}.}

\item{ref_date_col}{Character. Reference-date column in both panel tables.
Default \code{"ref_date"}.}

\item{start_date_col}{Character. Contract start-date column. Default \code{"start_date"}.}

\item{end_date_col}{Character. Contract end-date column. Default \code{"end_date"}.}

\item{contract_type_col}{Character. Contract-type column. Default \code{"contract_type_code"}.}

\item{status_col}{Character. Personnel status column. Default \code{"status"}.}
}
\value{
data.table with \code{group_cols} (if specified) and \code{hiring_rate} column.
}
\description{
Uses \code{govhr::detect_personnel_event()} to detect hire events across the
full historical panel, joins them to contract data to recover grouping
variables, computes per-group hiring rates (hires / active stock) for each
snapshot, and returns the mean rate per group.
}
