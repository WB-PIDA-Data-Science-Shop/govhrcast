% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exit_core.R
\name{estimate_historical_exit_rates}
\alias{estimate_historical_exit_rates}
\title{Estimate Historical Non-Retirement Exit Rates from Panel Data}
\usage{
estimate_historical_exit_rates(
  panel_contract_dt,
  panel_personnel_dt,
  group_cols = NULL,
  freq = "year",
  personnel_id_col = "personnel_id",
  ref_date_col = "ref_date",
  start_date_col = "start_date",
  end_date_col = "end_date",
  contract_type_col = "contract_type_code",
  status_col = "status"
)
}
\arguments{
\item{panel_contract_dt}{data.table.  Full panel of contract data (all
\code{ref_date} snapshots).}

\item{panel_personnel_dt}{data.table.  Full panel of personnel data.}

\item{group_cols}{Character vector or \code{NULL}.  Columns to group by
(e.g. \code{"est_id"}).  Pass \code{NULL} for an overall (ungrouped) rate.}

\item{freq}{Character.  Frequency passed to
\code{govhr::detect_personnel_event()}.  Default \code{"year"}.}

\item{personnel_id_col}{Character.  Default \code{"personnel_id"}.}

\item{ref_date_col}{Character.  Default \code{"ref_date"}.}

\item{start_date_col}{Character.  Default \code{"start_date"}.}

\item{end_date_col}{Character.  Default \code{"end_date"}.}

\item{contract_type_col}{Character.  Default \code{"contract_type_code"}.}

\item{status_col}{Character.  Default \code{"status"}.}
}
\value{
data.table with \code{group_cols} (if specified) and
\code{exit_rate} column.
}
\description{
Uses \code{govhr::detect_personnel_event(event_type = "fire")} to identify
non-retirement attrition events (voluntary resignation, dismissal, contract
non-renewal) across the full historical panel.  Computes
\code{exit_rate = n_exits / n_active} per group per panel snapshot, then
returns the mean rate per group.
}
